import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#4caf50', // Verde, por exemplo
        },
        secondary: {
            main: '#ff9800', // Laranja
        },
    },
});

export default theme;
